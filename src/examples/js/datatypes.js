/****** DATA TYPES ********/

const numberOfAttendes = 15; //number

const message = 'hello React School!'; //string

const isReactSchoolToday = true; //boolean

const teachers = ['Jorge', 'Gera']; //Array, an array can contain any data type even more arrays

let hello; //undefined, const cannot be used to declare an undefined variable

const nothing = null; //Null

//Object
const teacher = {
    name: 'Jorge',
    age: 30,
    skills: ['JS', 'React', 'TDD']
}